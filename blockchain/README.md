# Degree Verification Blockchain

This module handles smart contracts for issuing and verifying degree certificates
using Ethereum blockchain.

## Commands
- npm install
- npx hardhat compile
- npx hardhat test
- npx hardhat run scripts/deploy.js --network sepolia
